<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meu Portfólio</title>
    <style>
        /* Reset básico */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Corpo da página */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f4f8;
            color: #333;
            line-height: 1.6;
        }

        /* Cabeçalho */
        header {
            background-color: #6C63FF;
            color: white;
            text-align: center;
            padding: 20px 0;
        }

        /* Seção Sobre Mim */
        #sobre {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 30px 0;
        }

        .nome h2 {
            font-size: 2em;
            text-align: center;
            margin-bottom: 20px;
        }

        .sobre-mim {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            max-width: 900px;
            margin-top: 20px;
        }

        .foto img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 5px solid #6C63FF;
            margin-right: 30px;
        }

        .descricao {
            max-width: 600px;
        }

        .descricao h3 {
            font-size: 1.5em;
            color: #6C63FF;
            margin-bottom: 10px;
        }

        .descricao p {
            font-size: 1.1em;
            line-height: 1.6;
        }

        /* Seção Projetos */
        #projetos {
            background-color: #fff;
            padding: 30px;
            text-align: center;
        }

        #projetos h2 {
            color: #6C63FF;
            margin-bottom: 30px;
        }

        .projeto-container {
            display: flex;
            justify-content: space-between;
            gap: 20px;
        }

        .projeto {
            background-color: #f9f9f9;
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 10px; /* Caixas mais quadradas */
            width: 48%;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            height: 250px; /* Altura fixa para manter a caixa quadrada */
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .projeto h3 {
            color: #6C63FF;
            margin-bottom: 10px;
        }

        .projeto p {
            font-size: 1em;
            flex-grow: 1; /* Isso garante que o texto não ultrapasse o limite da caixa */
        }

        .projeto:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Seção Contato */
        #contato {
            background-color: #fff;
            padding: 30px;
            text-align: center;
        }

        #contato form {
            max-width: 500px;
            margin: 0 auto;
            text-align: left;
        }

        #contato label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        #contato input, #contato textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }

        #contato button {
            width: 100%;
            padding: 10px;
            background-color: #6C63FF;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        #contato button:hover {
            background-color: #5a54e1;
        }

        /* Rodapé */
        footer {
            background-color: #6C63FF;
            color: white;
            text-align: center;
            padding: 10px 0;
            margin-top: 30px;
        }
    </style>
</head>
<body>

    <header>
        <h1>Bem-vindo ao meu Portfólio!</h1>
    </header>

    <!-- Seção Sobre Mim -->
    <section id="sobre">
        <div class="nome">
            <h2>[Seu Nome]</h2>
        </div>
        <div class="sobre-mim">
            <div class="foto">
                <img src="sua-foto.jpg" alt="Minha Foto" />
            </div>
            <div class="descricao">
                <h3>Sobre Mim</h3>
                <p>Olá, sou [Seu Nome], um desenvolvedor web apaixonado por criar soluções digitais. Tenho experiência em diversas tecnologias como HTML, CSS, JavaScript e React. Busco sempre melhorar minhas habilidades e enfrentar novos desafios.</p>
            </div>
        </div>
    </section>

    <!-- Seção Projetos -->
    <section id="projetos">
        <h2>Meus Projetos</h2>
        <div class="projeto-container">
            <div class="projeto" id="projeto1">
                <h3>Projeto 1</h3>
                <p>Descrição do projeto, o que foi desenvolvido e as tecnologias utilizadas.</p>
            </div>
            <div class="projeto" id="projeto2">
                <h3>Projeto 2</h3>
                <p>Descrição do projeto, o que foi desenvolvido e as tecnologias utilizadas.</p>
            </div>
        </div>
    </section>

    <!-- Seção Contato -->
    <section id="contato">
        <h2>Contato</h2>
        <form action="mailto:seu-email@example.com" method="post" enctype="text/plain">
            <label for="nome">Nome:</label>
            <input type="text" id="nome" name="nome" required>
            
            <label for="email">E-mail:</label>
            <input type="email" id="email" name="email" required>
            
            <label for="mensagem">Mensagem:</label>
            <textarea id="mensagem" name="mensagem" rows="4" required></textarea>
            
            <button type="submit">Enviar</button>
        </form>
    </section>

    <!-- Rodapé -->
    <footer>
        <p>Desenvolvido por [Seu Nome] &copy; 2025</p>
    </footer>

    <script>
        // Função para adicionar o efeito de zoom nos projetos ao passar o mouse
        document.querySelectorAll('.projeto').forEach(function(projeto) {
            projeto.addEventListener('mouseover', function() {
                projeto.style.transform = 'scale(1.05)';
                projeto.style.boxShadow = '0 4px 8px rgba(0, 0, 0, 0.1)';
            });

            projeto.addEventListener('mouseout', function() {
                projeto.style.transform = 'scale(1)';
                projeto.style.boxShadow = 'none';
            });
        });
    </script>
</body>
</html>
